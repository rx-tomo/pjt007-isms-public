/**
 * Auth Provider Interfaces
 *
 * This module exports authentication provider interfaces for abstraction
 * between different auth backends (Better Auth, Mock Auth).
 */

export * from './IAuthProvider'
