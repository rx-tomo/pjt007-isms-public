# 2025-11-10 進捗ログ（UCレビュー）
作成者: Codex / 2025-11-10

## 1. ハイライト
- Super Admin の Dev Login → `/ja/super-admin/organizations` で発生していた 500 エラーを再現し、`list_all_tenants` の曖昧列参照・`user_profiles` RLS を修正するマイグレーション（`20251110100050_fix_list_all_tenants.sql`, `20251110113000_super_admin_profile_access.sql`）を追加。Edge Function `tenant-admin` を `supabase functions serve tenant-admin --env-file supabase/.env` で起動した状態で動作確認。
- Supabase 側のロール整合性を取り戻すため、`20251110134500_user_role_enum.sql` で `user_role` enum を導入し、`user_profiles` / `organization_invitations` の role カラムと `get_user_role()` / `user_has_role()` を enum ベースへ切り替え。`audit_logs.scope` も型付きで扱えるようになったため、Super Admin 向け監査 UI との整合性を再確認。
- UC-02/05/07 の QA Plan と実装を棚卸しし、Billing Portal のプラン変更後同期、デモリスク 5 件表示、監査オペレーター手順書など未完了の観点を `docs/02-project/12_uc-checklist.md` に反映。
- `docs/02-project/10_plan-tracking.md` / `docs/handoff/2025-11-10_handoff.yaml` を更新し、次スプリントへ引き継ぐべき QA / 運用タスク (#10, #11) を明文化。

## 2. UC別差分（主要項目）
| UC | 現状 | 差分/課題 | 次アクション |
| --- | --- | --- | --- |
| ロール: Super Admin | Dev Login & Playwright は通るが、Edge Function 未起動だと API 503。 | `tenant-admin` のローカル起動手順と `SUPABASE_SERVICE_ROLE_KEY` 取り扱いが Runbook になかった。`list_all_tenants` も曖昧参照で 500。 | Runbook/Checklist に supabase functions serve コマンドを追記済。マイグレーション適用を `supabase db push` に含める。 |
| UC-02 課金 | チェックリストはすべて ✅ だったが、Billing Portal → `sync-subscription` の再同期とオペレーター Runbook が未整備。 | Portal でのキャンセル/変更後に Home/Settings の差分が残ると復旧方法が不明。 | Checklist を [ ] へ戻し、オペレーター手順書作成と自動/手動 QA をタスク #10 として管理。 |
| UC-05 リスク | `npm run seed:risks-demo` が複数リスクを投入するが、Dev Login 後に Seed を流していない組織では 1 件のまま。Operator 手順書も「作成予定」。 | QA 手順が Seed 依存である旨が docs に未記載。ユーザーから 5 件表示されないとの問い合わせあり。 | Seed 実行 → `/ja/risks` で 5 件表示 → Excel export の流れを Checklist/Runbook に追加、Operator ドキュメントを作成。 |
| UC-07 監査 | 自動テストはグリーンだが、Auditor / System Operator 用手順書が未作成。 | Dev Login 以外の手動 QA フローを共有できておらず、証跡容量確認や RBAC 変更手順が属人化。 | Operator ドキュメントを docs/05-quality に作成し、QA Plan の「作成予定」を解消。 |
| UC-09 設定・権限 | ロール単位の RBAC は Playwright でカバー済みだが、部門スコープのアクセス制御 (Issue #64/#74) は未着手。 | UC Checklist では ✅ だが、部門フィルタ連動の実 RBAC が不足。 | Checklist に [ ] で追記し、Plan Tracking Item #9 に紐付け。 |

## 3. 実行コマンド（一部抜粋）
- `supabase functions serve tenant-admin --env-file supabase/.env`（Edge Function 起動）
- `PGPASSWORD=postgres psql -h 127.0.0.1 -p 55082 -U postgres -d postgres -f supabase/migrations/20251110100050_fix_list_all_tenants.sql`
- `PGPASSWORD=postgres psql -h 127.0.0.1 -p 55082 -U postgres -d postgres -f supabase/migrations/20251110113000_super_admin_profile_access.sql`
- `curl -s -i 'http://127.0.0.1:55081/functions/v1/tenant-admin' -H "Authorization: Bearer <session>" -H "apikey: $NEXT_PUBLIC_SUPABASE_ANON_KEY"`（テナント一覧レスポンス確認）
- `rg -n "docs/02-project/0" docs`（旧パス参照の洗い出し）

## 4. 更新したドキュメント / ファイル
- `docs/02-project/12_uc-checklist.md`: UC-02/05/07/09 のチェック状況更新、新規タスク追記、Super Admin Edge Function 手順を明記。
- `docs/02-project/10_plan-tracking.md`: 2025-11-10 サマリー追加、未対応 #2 状態更新、未対応 #10 #11 を追加。
- `docs/handoff/2025-11-10_handoff.yaml`: 現在の環境状態・オープン TODO・Super Admin 運用メモを追記。
- `docs/02-project/archive/90_2025-11-10_progress.md`（本ファイル）: レビュー結果の記録。
- `supabase/migrations/20251110100050_fix_list_all_tenants.sql`, `supabase/migrations/20251110113000_super_admin_profile_access.sql`: Super Admin 関連の DB 修正を追加。
- `supabase/migrations/20251110134500_user_role_enum.sql`: `user_role` enum 化と `audit_logs.scope` 型付けにより、開発/QA/Dev Login でのロール差異を吸収。

## 5. 次アクション
1. Billing Portal 変更/キャンセルの再現手順を Playwright or CLI で固め、UC-02 QA Plan と Operator 手順へ反映（Plan Tracking #10）。
2. `npm run seed:risks-demo` → `/ja/risks` 5 件表示のスクリーンショット取得、Operator 手順書 2 本を作成（Plan Tracking #11）。
3. 監査オペレーター手順（Auditor/System Operator）を docs/05-quality に追加し、Edge Function `tenant-admin` のデプロイ/serve 手順を README にリンク。
4. 部門スコープ RBAC (Issue #64/#74) の実装計画を整理し、UC-09 ✅ に依存するタスクへ影響範囲を共有。

## 6. 2025-11-12 追記（Issue #151: Billing Portal Runbook）
- `npm run qa:stripe:portal` をローカル Supabase/Next 環境で完走させ、Portal アップグレード → `/api/stripe/sync-subscription` → キャンセルのログを `test-results/qa-stripe-portal-sync-2025-11-12T04-58-35-251Z.json` に保存。`pricing_plans` の `display_order` と `stripe_price_id` を整備し、Enterprise プラン用に Stripe 価格 `price_1SSW8IEA5dswKoHjhXVv8nzV` を追加。
- CLI が新しいテーブル制約で失敗していたため、`scripts/qa-stripe-portal-sync.js` の初期組織 `subscription_plan`/`subscription_status` を (`trial`, `active`) に修正。今後の QA でも check constraint に阻まれず実行できる。
- Runbook `docs/06-operations/billing-and-data-operations.md` に Portal 変更/キャンセル手順、`curl` コマンド例、異常系トリアージ、連絡テンプレート、更新履歴を追加。UC-02 チェックリスト備考と `docs/handoff/2025-11-10_handoff.yaml` を同時更新し、handoff outstanding `uc02-portal-sync` をクローズした。
