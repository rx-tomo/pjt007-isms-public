# 2025-10-23 Stripe QA & UC Review 進捗ログ

## サマリー
- Stripe テスト鍵を用いた Checkout → Webhook → Supabase 同期を `npm run qa:stripe` で再検証し、ローカル環境の課金ユースケースがモック依存なく通ることを確認した。
- Supabase トリガー `update_organization_subscription_status` の最新実装を確認し、Stripe Subscription のステータス／プランが `organizations` とドキュメント保持期間へ正規化されることを QA ログで確認した。
- UC チェックリストと QA メモをアップデートし、Stripe フローの完了条件とフォローアップ項目（Billing Portal QA の完了記録と Webhook 異常系の自動化）を反映した。

## 実施項目
1. **Stripe Checkout QA 実施**
   - `stripe listen --forward-to http://localhost:3007/api/stripe/webhook` を起動し、Webhook 署名シークレットを取得。
   - `npm run qa:stripe` を実行し、Checkout セッション (`cs_test_a1A37Drhp5h2qSjkF1UaOiCwiIa1oK7DBALBccJGvhrzZ8oW0HfwVk5Lou`) が成功すること、Stripe サブスクリプション (`sub_1SLJivEA5dswKoHjJ7q9u4FI`) が作成されることを確認。
   - QA スクリプト出力と Stripe CLI ログで Supabase `subscriptions` レコード作成と Webhook 処理完了を確認し、`organizations` 更新フックがエラーなく終了することを確認。
2. **サブスクリプション同期トリガーの動作確認**
   - `update_organization_subscription_status` の更新内容（Stripe ステータスの正規化と `documents.retention_delete_at` の自動調整）をレビューし、Webhook 実行ログで適用されることを確認。
3. **ドキュメント棚卸し**
   - `docs/02-project/12_uc-checklist.md` と `docs/05-quality/uc-validation-20250918.md` を更新し、Stripe 実行ログと残課題を反映。
   - `docs/02-project/10_plan-tracking.md` に 2025-10-23 の更新サマリーを追記し、ウォーキングスケルトンの最新状態と同期。

## 使用コマンド・ログ
- `stripe listen --forward-to http://localhost:3007/api/stripe/webhook`
- `npm run qa:stripe`

## フォローアップ（2025-11-05）
1. **Billing Portal 変更／キャンセル QA**: Stripe Billing Portal からプラン変更（`Basic` → `Enterprise`）と即時キャンセルを実施し、`subscriptions` / `organizations.subscription_plan` / `payment_history` の更新が行われること、`update_organization_subscription_status` トリガーが保持期間を再計算することを確認した。手順と確認項目を `docs/05-quality/uc-validation-20250918.md` に追記し、進捗ログに反映した。
2. **Webhook 異常系 QA**: `npm run qa:webhook:abnormal` を拡張し、(a) 無効署名で 400 を返すこと、(b) `/api/stripe/mock/webhook/toggle-500?count=1` で 500 を強制後にリトライで 200 へ復旧すること、(c) `X-Test-Event-Id` を付与した重複イベントが `stripe_events.processed=true` で冪等処理されることを確認した。
3. **ホームダッシュボード異常系 QA**: `npm run qa:home` にフェールセーフモードを追加し、`tests/e2e/home-failsafe.spec.ts` で警告バナーと統計の暫定値表示を確認した。`docs/05-quality/uc-validation-20250918.md` の UC-03 TODO を完了扱いに更新。

## 未解決・TODO
- なし
