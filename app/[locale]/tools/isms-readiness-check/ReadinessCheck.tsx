'use client';
import { useState } from 'react';
import { useTranslations } from 'next-intl';
export default function ReadinessCheck() { const t = useTranslations('publicSeo.readiness'); const items = ['scope', 'owner', 'evidence', 'review'] as const; const [checked, setChecked] = useState<Record<string, boolean>>({}); const complete = items.filter((item) => checked[item]).length; const remaining = items.length - complete; return <section className="mt-10 rounded-xl border border-border bg-surface p-6 shadow-sm" aria-labelledby="readiness-check-heading"><h2 id="readiness-check-heading" className="text-2xl font-semibold text-text-primary">{t('checkTitle')}</h2><p className="mt-2 text-text-secondary">{t('progress', { complete, total: items.length, remaining })}</p><div className="mt-6 space-y-3">{items.map((item) => <label key={item} className="flex cursor-pointer items-start gap-3 rounded-lg border border-border p-4"><input type="checkbox" checked={Boolean(checked[item])} onChange={() => setChecked((current) => ({ ...current, [item]: !current[item] }))} className="mt-1 h-4 w-4 accent-accent" /><span className="text-text-primary">{t(`items.${item}`)}</span></label>)}</div><p className="mt-6 text-sm leading-6 text-text-muted">{t('memoryOnly')}</p></section>; }
